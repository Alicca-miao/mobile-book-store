// const mysql = require('mysql2');
// const { mysqlConfig, promisePool } = require('../config/database');

// // 查询函数，返回 Promise
// const proo = async function(sql, option) {
//   try {
//     // 执行查询
//     const [results, fields] = await promisePool.query(sql, option);
//     return { results, fields };
//   } catch (error) {
//     // 处理错误
//     throw error;
//   }
// };

// module.exports = proo;
